import type { Notification } from "@/types/notification";
type Props = { notification: Notification; onRead: (id: string) => void };
export function NotificationCard({ notification, onRead }: Props) { return <article className={`notification-card ${notification.status.toLowerCase()}`}><div className="notification-card-heading"><div><span className="notification-label">DEVELOPMENT NOTIFICATION</span><h3>{notification.title}</h3></div><span className="notification-priority">{notification.priority}</span></div><p>{notification.message}</p><dl><div><dt>Source</dt><dd>{notification.source}</dd></div><div><dt>Type</dt><dd>{notification.type}</dd></div><div><dt>Status</dt><dd>{notification.status}</dd></div><div><dt>Created</dt><dd>{notification.createdAt}</dd></div></dl>{notification.status !== "READ" ? <button className="task-action-button" onClick={() => onRead(notification.id)}>Mark as read</button> : null}</article>; }
