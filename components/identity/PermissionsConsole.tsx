import { permissionMatrix } from "@/lib/security";

export function PermissionsConsole() { return <div className="dashboard-content permissions-content"><div className="dashboard-intro"><div><p className="page-kicker">AUTODOC / security foundation</p><h1>Permission matrix</h1><p className="page-description">Read-only development policy for explicit family, member, memory, and system access.</p></div><div className="system-clock"><span className="clock-dot" />POLICY <strong>EXPLICIT / READ ONLY</strong></div></div><div className="permission-grid">{Object.entries(permissionMatrix).map(([role, permissions]) => <section className="permission-role panel" key={role}><div className="panel-heading"><div><p className="panel-eyebrow">Role</p><h2>{role}</h2></div><span className="timeline-status">{permissions.length.toString().padStart(2, "0")}</span></div><ul>{permissions.map((permission) => <li key={permission}>{permission}</li>)}</ul></section>)}</div><p className="permission-note">Permission checks occur in service/API boundaries. UI visibility is not used as security.</p></div> }
